package com.emailscheduler.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MailConfig {
    // JavaMailSender is automatically configured by Spring Boot starter mail 
    // based on configurations in application.properties.
}
